import { h } from 'preact';

import { ThemeComponent } from './theme';

interface Props {

}

export const SettingComponent = (props: Props) => {

  return (
    <div>
      <ThemeComponent />
    </div>
  )
};